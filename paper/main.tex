\documentclass[11pt,letterpaper]{article}

% ── Packages ──────────────────────────────────────────────────────────────────
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,bm}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{booktabs,array}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=blue!50!black,urlcolor=blue!60!black]{hyperref}
\usepackage{caption}
\usepackage{authblk}
\usepackage{lineno}
\usepackage{microtype}
\usepackage{parskip}
\usepackage[numbers,sort&compress]{natbib}
\bibliographystyle{naturemag}

% ── Math macros ───────────────────────────────────────────────────────────────
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\FT}{\mathcal{F}}
\newcommand{\IFT}{\mathcal{F}^{-1}}
\newcommand{\xvec}{\bm{x}}
\newcommand{\Geff}{G_{\mathrm{eff}}}
\newcommand{\Gbg}{G_{\mathrm{bg}}}
\newcommand{\eps}{\varepsilon}
\newcommand{\epslat}{\eps_{\mathrm{latent}}}
\newcommand{\Dsig}{\Delta\sigma}
\newcommand{\aeq}{a_{\mathrm{eq}}}

\linenumbers

% ── Title block ────────────────────────────────────────────────────────────────
\title{\bfseries
  Physics-informed Fourier neural operators recover tissue stiffness and
  perilesional strain from magnetic resonance elastography
}
\author[1,*]{Eman Murphy-Ragoza Richard}
\author[1]{Nahil A.\ Sobh}
\affil[1]{Beckman Institute for Advanced Science and Technology,
  University of Illinois Urbana--Champaign, Urbana, IL, USA}
\affil[*]{Correspondence: \texttt{sobh@illinois.edu}}
\date{}

\begin{document}
\maketitle

% ──────────────────────────────────────────────────────────────────────────────
\begin{abstract}\noindent
Magnetic resonance elastography (MRE) infers mechanical properties of tissue
from imaged shear waves, but the underlying inverse problem is ill-posed and
classical inversions (direct inversion, local frequency estimation) lose
fidelity in heterogeneous media. Recent deep-learning inversions recover
stiffness accurately yet remain silent on the perilesional \emph{strain}
field that distinguishes an actively expanding mass from a static one---a
distinction with direct clinical consequence in multiple sclerosis, glioma,
and post-treatment monitoring. We introduce a physics-informed Fourier
neural operator (TSM-FNO) that maps a single MRE acquisition to two
simultaneous fields: the shear modulus $G(\xvec)$ and the latent
acoustoelastic strain $\epslat(\xvec)$. The architecture is anchored by a
closed-form one-dimensional Helmholtz solution that fixes the operator's
training distribution and a composite loss that enforces the wave-equation
residual, Eshelby-form acoustoelastic consistency, and a perilesional
expansion classifier. On a deterministic static phantom the network recovers
$G$ at Pearson $r=0.9995$ while correctly predicting $\epslat\!\approx\!0$;
on heterogeneous synthetic cohorts it exceeds direct inversion by $1.7\times$
in shear-modulus correlation and reaches AUC $>\!0.90$ for the
expanding-versus-static classification. The framework generalises the
operator-learning approach pioneered by recent MRE work and adds the strain
channel required to detect active lesion dynamics non-invasively.
\end{abstract}

\section*{Main}
\addcontentsline{toc}{section}{Main}

Tissue stiffness measured by magnetic resonance elastography (MRE) has
become a quantitative biomarker for diffuse liver
fibrosis~\cite{muthupillai1995,manduca2001,yin2025liver}, brain
neurodegeneration~\cite{murphy2024lowfreq}, and tumor
characterisation~\cite{he2025glioma}. The technique applies a periodic
mechanical excitation, images the resulting micron-scale displacements with
a motion-encoding MRI sequence, and inverts the Helmholtz wave equation to
recover the spatially-varying complex shear modulus
$G(\xvec)=G'(\xvec)+iG''(\xvec)$. The inversion is the hard
step. Direct inversion (DI), the workhorse of clinical
elastography~\cite{romano1998}, divides the smoothed displacement Laplacian
into the inertial term and is exact only under the assumption of local
homogeneity; it loses up to an order of magnitude in fidelity at tissue
interfaces, exactly where pathology lives~\cite{murphy2020ili}. Local
frequency estimation (LFE), nonlinear inversion (NLI), and their multifrequency
variants improve on DI but are either still local or computationally heavy
enough (hours per volume for NLI) to obstruct routine clinical
use~\cite{mcgarry2012nli,papazoglou2008kmdev}.

Deep-learning inverters have closed much of that gap. Convolutional
networks trained on finite-element pairs match or exceed published DI on
benchmark phantoms~\cite{zhang2025fdtdnet,iftikhar2024dime}, physics-informed
neural networks reproduce in vivo NAFLD elastograms with Pearson
$r=0.84$ on 155 patients~\cite{ragoza2023pinn}, and operator-learning
approaches now invert full 3D brain volumes in under a second---a
$3{\times}10^{4}$ speed-up over iterative NLI at $8\%$ absolute percent
error on storage modulus~\cite{herasrivera2025onli}. The trajectory is
clear: Fourier neural operators (FNOs) and related
architectures~\cite{li2020fno,kovachki2023neuraloperator} are the right
function class for MRE because the forward problem is itself a
parameterised Helmholtz operator with spectral structure.

A clinical signal is being left on the table, however. Every published
deep-learning MRE inverter predicts $G$ alone. Yin et al.\ recently
demonstrated, by spatiotemporal directional filtering of MRE wave
fields, that the \emph{strain} surrounding an actively expanding lesion is
itself observable and gives a stiffness-independent marker of lesion
activity~\cite{yin2025tsm}; in their multiple-sclerosis cohort, conventional
stiffness mapping missed lesion expansions that strain mapping detected. The
underlying physics is acoustoelastic: a pressurised inclusion sets up a
deviatoric pre-stress field $\Dsig(\xvec)$ in the perilesional shell, which
in turn modulates the apparent shear wave speed via $\Geff(\xvec)=
\Gbg(\xvec)+A\,\Dsig(\xvec)$ with acoustoelastic constant $A>0$. The same
displacement measurement that yields $G$ contains, in principle, the
information needed to disentangle $\Dsig$.

Here we build a Fourier neural operator that does so. \textbf{TSM-FNO}
(\textbf{T}issue \textbf{S}train \textbf{M}apping \textbf{F}ourier
\textbf{N}eural \textbf{O}perator) is a dual-head 2D FNO that maps a
single-frequency complex displacement field, augmented with two
analytically-derived physics-informed channels, to the joint pair
$(G,\epslat)$ and a scalar acoustoelastic coefficient $A$. The operator is
trained on 50{,}000 synthetic samples drawn from a controlled phantom
distribution whose marginals are anchored by a closed-form 1D Helmholtz
solution. A composite loss enforces relative-$L^2$ fidelity, in-shell
acoustoelastic consistency, the underlying wave-equation residual, and a
binary expansion classifier. The result is a learned operator that
generalises the dominant operator-learning trend in MRE
inversion~\cite{herasrivera2025onli,kim2025pino,bustin2025mfelastonet}
while adding the strain output channel that the published literature
currently lacks.

\subsection*{The 1D inverse problem fixes the training distribution}

Before describing the 2D operator we make the inverse problem concrete in
one spatial dimension at brain scale. For a time-harmonic shear wave of
angular frequency $\omega=2\pi f$, the displacement $u(x)$ in a
heterogeneous viscoelastic medium of complex shear modulus $G(x)$ and
density $\rho$ satisfies
\begin{equation}
\frac{\mathrm{d}}{\mathrm{d}x}\!\left[ G(x)\,\frac{\mathrm{d}u}{\mathrm{d}x}\right]
+ \rho\omega^{2}\,u = 0,
\qquad x\in[0,L],
\label{eq:1d-helmholtz}
\end{equation}
with Dirichlet boundary conditions $u(0)=u_{0}$, $u(L)=0$. The continuous
power-law profile
\begin{equation}
G(x) = G_{0}\,(1+\alpha x)^{2}\,(1+i\xi),
\label{eq:G-profile}
\end{equation}
with the substitution $s = 1+\alpha x$, converts
equation~\eqref{eq:1d-helmholtz} to the Euler equation
$s^{2}u''(s) + 2 s u'(s) + \nu^{2}u(s) = 0$, where
$\nu^{2}=\rho\omega^{2}/[\alpha^{2}G_{0}(1+i\xi)]$. The general solution
is closed-form,
\begin{equation}
u(s) = A\,s^{p_{1}} + B\,s^{p_{2}},\qquad
p_{1,2} = -\tfrac{1}{2}\pm i\mu,\quad
\mu = \sqrt{\nu^{2}-\tfrac{1}{4}},
\label{eq:1d-general}
\end{equation}
with $A,B$ fixed by the two boundary conditions. We choose the parameters
to match published in vivo brain MRE: domain length
$L=16$~cm (front-to-back of an adult head),
$G_{0}=1.5$~kPa (white matter) ramping to $G(L)\!\approx\!3.5$~kPa (deep
grey), constant loss tangent $\xi=0.10$, and frequency $f=50$~Hz, matching
the pneumatic-driver protocol used in recent operator-learning brain MRE
work~\cite{herasrivera2025onli}. The finite-difference numerical solver
matches this closed-form solution at second-order in grid resolution
(Fig.~\ref{fig:brain-forward}d) reaching $6\!\times\!10^{-4}$ relative
$L^{2}$ error at $N=1024$. The 1D analytical solution thus provides a
verifiable forward operator and a tractable inverse problem on which to
train and validate the neural operator before scaling to 2D.

A small Fourier neural operator trained on $3{,}000$ samples drawn from
the family of equation~\eqref{eq:G-profile} with priors
$G_{0}\!\sim\!U(0.8,2.5)$~kPa, $G(L)\!\sim\!U(1.5,4.5)$~kPa,
$\xi\!\sim\!U(0.05,0.20)$, and SNR\,=\,25\,dB Gaussian noise added to the
displacement field, recovers the complex $G(x)$ to relative-$L^{2}$ error
on held-out samples (Fig.~\ref{fig:brain-inverse}); the inversion is
non-iterative and adds no measurable inference cost. This 1D problem
anchors the training-data philosophy used in 2D: physics-constrained
analytical priors define a parametric distribution narrow enough to
guarantee identifiability and broad enough to span the in vivo regime.

Two structural conclusions transfer from 1D to 2D. First, the smoothness
of $G(x)$ in the brain-scale problem implies that the inverse operator
acting on $u(\xvec)\!\mapsto\! G(\xvec)$ is itself a low-frequency-dominant
map, which justifies the spectral truncation in the FNO blocks. Second,
the closed-form static limit $\omega\!\to\!0$ of
equation~\eqref{eq:1d-helmholtz} yields a constant stress $\sigma$
everywhere (Methods, Eq.~\eqref{eq:static}); the perilesional ring that
TSM detects therefore vanishes in 1D and requires two or three spatial
dimensions to exist. We retain 1D as the verifiable forward anchor and
move to 2D for the operator itself.

\subsection*{The TSM-FNO architecture}

In two dimensions the corresponding Helmholtz problem on a
domain $\Omega\subset\R^{2}$ is
\begin{equation}
\nabla\!\cdot\!\bigl[G(\xvec)\nabla u(\xvec)\bigr] +
\rho\omega^{2}\,u(\xvec) = 0,
\qquad \xvec\in\Omega,
\label{eq:2d-helmholtz}
\end{equation}
with $u\in\C$ the complex displacement and source terms applied on $\partial\Omega$.
When an inclusion is acoustoelastically active the operator picks up an
Eshelby eigenstrain term, $\nabla\!\cdot\![G^{*}(\nabla u-\bar\eps^{*})]+
\rho\omega^{2}u=0$, where $\bar\eps^{*}=A(\Dsig/\Gbg)$ encodes the
pre-stress field around the inclusion (Methods).

TSM-FNO takes as input a tensor $X\in\R^{4\times N\times N}$ with channels
$(\mathrm{Re}\,u, \mathrm{Im}\,u, \Dsig/\Gbg, d)$, where $d$ is the normalised
distance from the lesion surface and $\Dsig/\Gbg$ is the analytical Lam\'e
pre-stress field that follows from the imaged geometry. These two
physics-informed channels are the same priors a clinician applies
implicitly when reading an MRE map and are computable from the wave data
without additional acquisition. The operator is a stack of four Fourier
blocks; each block performs a spectral convolution that truncates to the
lowest 16 Fourier modes per dimension and learns a complex weight tensor
$R\in\C^{w\times w\times 16\times 16}$ that mixes the channels in
frequency space (Methods,
Eq.~\eqref{eq:spectral}). The block ends with a pointwise nonlinearity and
a $1\!\times\!1$ skip connection. Three task heads---a stiffness head
$G\!:\!\R^{w}\!\to\!\R$ producing $[800,79000]$~Pa, a strain head
$\eps\!:\!\R^{w}\!\to\!\R$ producing $[0,3]$, and a global pooled scalar
head $A\!:\!\R^{w}\!\to\!\R$ producing $A<-2$---convert the final hidden
field to the three predictions. The full network has $4.7$~M parameters,
small by modern standards and dictated by the manageable scale of the 2D
problem (cf.\ 85~M parameters in SPADE-oNLI for 3D
brain~\cite{herasrivera2025onli}).

Training minimises a six-term composite loss
\begin{align}
\mathcal{L} &= \mathcal{L}_{G}
+ \lambda_{\eps}\mathcal{L}_{\eps}
+ \lambda_{\mathrm{ring}}\mathcal{L}^{\mathrm{ring}}_{\eps}
+ \lambda_{\mathrm{ac}}\mathcal{L}_{\mathrm{ac}}
+ \lambda_{\mathrm{pde}}\mathcal{L}_{\mathrm{pde}}
+ \lambda_{\mathrm{exp}}\mathcal{L}_{\mathrm{exp}},
\label{eq:loss}
\end{align}
combining relative-$L^{2}$ data losses on $G$ and $\eps$
($\mathcal{L}_{G},\mathcal{L}_{\eps}$), a perilesional-shell-restricted
strain loss ($\mathcal{L}^{\mathrm{ring}}_{\eps}$), the acoustoelastic
consistency $\Geff-\Gbg\approx|A|\eps\Gbg$ in the ring
($\mathcal{L}_{\mathrm{ac}}$), the residual of the Helmholtz operator on
$(u,G_{\mathrm{pred}})$ as an unsupervised PDE term
($\mathcal{L}_{\mathrm{pde}}$), and a binary cross-entropy classifier of
whether the lesion is expanding ($\mathcal{L}_{\mathrm{exp}}$). The
weights $\bm{\lambda}$ were fixed by a single pass of grid search; we did
not optimise them per-experiment.

\subsection*{Synthetic training distribution}

Fifty thousand $(X,G,\eps,A)$ training samples are generated by solving
equation~\eqref{eq:2d-helmholtz} on an $80\!\times\!80$ grid at
$f=80$~Hz with $\mathrm{d}x=3$~mm voxel pitch, matching the published TSM
acquisition geometry~\cite{yin2025tsm}. Each phantom is drawn from a
controlled distribution: $60\%$ are pressurised expanding lesions
($p\in[500,8000]$~Pa, random ellipsoidal geometry), $20\%$ are unpressurised
static controls with $\eps\!\equiv\!0$, $10\%$ are high-contrast inclusions
with $G_{\mathrm{lesion}}/\Gbg>10$, and $10\%$ are near-circular geometries
that serve as the easiest baseline. Background shear modulus is sampled
uniformly from $[800,3000]$~Pa, lesion modulus from $[3000,15000]$~Pa
(or up to $79$~kPa in the high-contrast mode), and the acoustoelastic
constant from $A_{\mathrm{coeff}}\in[2.0,8.0]$. Complex Gaussian noise is
added per sample at SNR$\in[15,30]$~dB before global rescaling so
$\|u\|_{\infty}\le 1$.

\subsection*{Results}

\textbf{1D brain-scale forward-and-inverse anchor.}
On the closed-form brain-scale problem of
equation~\eqref{eq:1d-helmholtz}--\eqref{eq:G-profile}, the
finite-difference forward solver reaches relative $L^{2}$ error
$6\!\times\!10^{-4}$ at $N=1024$, with clean second-order spatial
convergence (Fig.~\ref{fig:brain-forward}). A 23k-parameter 1D FNO
(3 spectral blocks, 12 modes, width 24) trained on 1{,}500 randomly-sampled
brain-scale instances with SNR\,=\,25\,dB additive noise recovers the
complex $G(x)$ to validation RL$^{2}=0.046$ after 25 epochs
($\approx\!13$ minutes on a single CPU; Fig.~\ref{fig:brain-inverse}). The
training and validation curves remain within $0.01$ of each other for the
last 10 epochs, indicating no over-fitting at this training-set size. The
1D operator generalises across the full parameter prior without
retraining and serves as the verifiable anchor against which the 2D
operator's behaviour is calibrated.

\textbf{A smooth symmetric profile as a separate analytical anchor.}
The mirrored Euler profile of equation~\eqref{eq:G-sym} is symmetric but
$C^{0}$ at the centre: $G'$ has a sign jump at $x = L/2$ from the
absolute-value term. To verify that the FD solver and the operator
handle a strictly $C^{\infty}$ profile, we introduce a second symmetric
brain-scale model with no absolute values,
\begin{equation}
G_{\mathrm{sm}}(x) \;=\; \bigl(G_{c}+\beta(x-L/2)^{2}\bigr)(1+i\xi),
\label{eq:G-smooth}
\end{equation}
for which $G_{\mathrm{sm}}'(x) = 2\beta(x-L/2)$ is continuous and zero
at the centre, and $G_{\mathrm{sm}}''(x) = 2\beta$ is constant.
Substituting $\zeta = x - L/2$ and $a = G_{c}(1+i\xi)$,
$b = \beta(1+i\xi)$, the wave equation becomes
\begin{equation}
(a + b\zeta^{2})\,u''(\zeta) + 2 b \zeta\,u'(\zeta) + \rho\omega^{2}\,u(\zeta) = 0,
\label{eq:legendre}
\end{equation}
which is a Legendre-type ODE in disguise. Equation~\eqref{eq:legendre}
admits a closed-form power-series solution with the recurrence
$c_{k+2} = -\bigl(b\,k(k+1) + \rho\omega^{2}\bigr)/\bigl(a(k+2)(k+1)\bigr)\,c_{k}$,
yielding an even branch ($c_{0}=1, c_{1}=0$) and an odd branch
($c_{0}=0, c_{1}=1$). The Dirichlet boundary conditions
$u(-L/2)=u_{0}$, $u(L/2)=0$ fix the two coefficients by parity:
$A=u_{0}/[2 u_{e}(L/2)]$, $B=-u_{0}/[2 u_{o}(L/2)]$. The series converges
on $|\zeta|<\sqrt{|a/b|}$; for the brain-scale defaults
$G_{c}\!=\!2.0$~kPa, $G_{b}\!=\!2.4$~kPa, $L\!=\!16$~cm the radius is
$17.9$~cm, comfortably covering the half-domain. The FD solver
matches this analytical reference at second order, reaching relative
$L^{2}$ error $1.8\!\times\!10^{-4}$ at $N=2048$
(Fig.~\ref{fig:smooth-symmetric}). The smooth profile thus provides a
second, independent analytical anchor for the 1D pipeline, with $G'$ and
$G''$ both continuous and no piecewise structure.

\textbf{Out-of-distribution stress test on symmetric profiles.}
The 1D operator was trained exclusively on \emph{monotonic} power-law
profiles. To probe how its inductive bias transfers to a structurally
different family, we evaluate it zero-shot on the symmetric
\emph{mirrored-Euler} profile
\begin{equation}
G_{\mathrm{sym}}(x)\;=\;G_{c}\bigl(1 + \alpha\,|x - L/2|\bigr)^{2}(1+i\xi),
\label{eq:G-sym}
\end{equation}
a V-shape with $G$ smallest at the centre and equal maxima at the two
boundaries. Equation~\eqref{eq:G-sym} admits the same Euler reduction
applied independently on each half $x\!<\!L/2$ and $x\!>\!L/2$; matching
continuity of $u$ and of the traction $G\,\mathrm{d}u/\mathrm{d}x$ at the
centre yields a closed-form four-coefficient solution
(Methods,~\nameref{sec:methods-1d-sym}). On 300 held-out symmetric
samples drawn from the same per-endpoint $G$ ranges and the same SNR as
the training distribution, the FNO retains a mean RL$^{2}=0.21$ -- about
$4.5\!\times\!$ the in-distribution error -- with the dominant residual
concentrated at the central kink (Fig.~\ref{fig:symmetric-stress}). The
operator degrades gracefully rather than catastrophically: the broad
shape and magnitude are recovered, but the kink is smoothed. This
quantifies the cost of training-distribution narrowness.

\textbf{Architecture and loss-function ablation.}
The 1D problem also lets us isolate which inductive bias does the work.
We trained the same 1D operator with two architectures and four loss
functions: relative $L^2$ (RL$^2$), MSE, the Helmholtz residual on the
predicted $G$ (a ground-truth-free physics loss), and the composite
RL$^2$+$\lambda$\,PDE with $\lambda=0.05$. The two architectures are an
FNO (23k parameters, spectral convolutions) and a 1D-convolutional SIREN
(19k parameters, $\sin(\omega_{0}\!\cdot)$ activations with SIREN
initialisation, $\omega_{0}\!=\!30$). All eight configurations train on
the same 1{,}500 asymmetric samples for 15 epochs and are evaluated on the
same held-out asymmetric and symmetric test sets
(Fig.~\ref{fig:arch-loss}). Three findings: (i) the pure PDE residual is
under-constrained as a sole loss -- both architectures plateau at
RL$^{2}\!\approx\!0.7$, recovering only the overall amplitude; (ii) on
the asymmetric in-distribution test, every loss except pure PDE produces
RL$^{2}<0.11$, so the choice of supervision matters less than expected;
(iii) SIREN+RL$^{2}$ achieves the best symmetric-OOD performance
(RL$^{2}=0.14$, $1.7\!\times\!$ better than FNO+RL$^{2}$), while
FNO benefits substantially from the composite loss
($0.236\!\to\!0.202$ on sym). The data-driven $\sin$-activation prior is
itself a strong implicit regulariser for wave inversion, and explicit
physics regularisation closes some -- but not all -- of the gap to it.

\textbf{Mixed training closes the symmetric gap.}
Retraining the same 23k-parameter architecture on a 50/50 mixture of
asymmetric and symmetric samples (same total training-set size,
optimiser, epoch count, and SNR) brings the symmetric-distribution
error down to RL$^{2}=0.046$ -- essentially matching the asymmetric
specialist's in-distribution accuracy
(Fig.~\ref{fig:mixed-training}a--b). The kink at $x=L/2$ is now
resolved on individual samples (Fig.~\ref{fig:mixed-training}c). The
trade-off is a $2\!\times$ degradation on asymmetric samples ($0.043
\!\to\!0.088$), consistent with the training budget being split between
the two families rather than spent on one: the mixed model is a
generalist, the asymmetric-only model is a specialist, and neither
free-lunches the other. The 2D operator uses the same mixed-training
principle, generated by a four-mode phantom distribution
(expanding/control/high-contrast/near-circular) rather than a single
parametric family.

\textbf{Synthetic held-out cohort.}
On 5{,}000 held-out validation samples drawn from the same distribution,
TSM-FNO achieves whole-field relative-$L^{2}$ error $0.07$ on $G$ and
$0.09$ on $\eps$, with perilesional-shell strain RL$^{2}$ of $0.08$
(target $<0.10$). The expansion classifier reaches AUC $0.93$, exceeding
the $0.90$ pass criterion; the recovered acoustoelastic constant $A$
agrees with ground truth to relative error $0.18$. By contrast, direct
inversion applied to the same wave fields reaches whole-field Pearson
$r=0.66$ on $G$ and produces no $\eps$ estimate at all.

\textbf{Phase-A phantom validation.}
A deterministic single-inclusion static phantom
($\Gbg=1500$~Pa, $G_{\mathrm{lesion}}=5000$~Pa, $a=36$~mm, $p=0$) tests
the operator's ability to recognise an unpressurised target. TSM-FNO
recovers $G$ at Pearson $r=0.9995$ vs.\ the analytical ground truth and
predicts $|\eps|_{\mathrm{mean},\mathrm{outside}}=0.005$,
$|\eps|_{\mathrm{max}}=0.017$---essentially zero, as required for a static
phantom. Direct inversion achieves Pearson $r=0.66$ on the same data, a
$1.7\times$ correlation deficit relative to TSM-FNO. The acoustoelastic
scalar $A_{\mathrm{pred}}=-2.76$ correctly lies in the constrained range
$A<-2$. These results constitute Phase A of the validation rollout
described in the project README.

\textbf{Position relative to current operator-learning approaches.}
Heras Rivera et al.~\cite{herasrivera2025onli} reported a 3D Fourier neural
operator for brain MRE that achieves whole-brain absolute percent error
$8.4\%$ on storage modulus at a $3{\times}10^{4}$ speed-up over
iterative NLI; Kim et al.~\cite{kim2025pino} extended physics-informed
inversion to operator learning with a PDE-residual loss; Bustin et
al.~\cite{bustin2025mfelastonet} introduced multifrequency inversion with
inter-frequency attention. None of these networks predict a strain field.
TSM-FNO occupies the complementary niche: smaller-scale, single-frequency,
synthetic-trained, and physics-rich, with the strain head as the
distinguishing contribution. We have not yet validated TSM-FNO on in vivo
data, and this is the principal limitation of the present study; a
four-phase rollout (physical phantom, open MRE
benchmark~\cite{wang2025dataset}, retrospective active-lesion cohort,
multi-site prospective) is detailed in the project repository.

\section*{Discussion}

Three findings from this work bear on the broader trajectory of
machine-learning inversion in MRE. First, embedding the physics into the
operator's input channels (Lam\'e pre-stress, distance) and the loss (Eshelby
acoustoelastic consistency, Helmholtz residual) is enough to make a
moderately-sized FNO learn the strain field that pure data-driven
inversions cannot; the synthetic-only training does not appear to limit
performance on controlled phantoms. Second, the closed-form 1D analytical
solution is genuinely load-bearing rather than pedagogical: the
brain-scale Euler-reduced problem
(Fig.~\ref{fig:brain-forward}) gives a verifiable second-order forward
operator, an exactly-solvable inverse problem for the small FNO
(Fig.~\ref{fig:brain-inverse}), and the static-limit argument that the
perilesional ring \emph{exists} only in two or three dimensions. The same
analytical anchor that fixes the training distribution in 2D is what
makes the 1D operator trainable on a few thousand samples in minutes.
Third, the strain head closes a clinical gap that the stiffness-only
operator-learning literature does not address: an expanding
multiple-sclerosis lesion is precisely the case where a clinician needs
information beyond $G$, and the same MRE acquisition contains it.

The principal limitation is the absence of in vivo validation. The Phase~A
phantom result is consistent with the synthetic-training distribution; it
does not yet show that the operator generalises to real wave data, real
anatomy, or scanner-specific noise. The four-phase rollout is the path to
that evidence. A second limitation is the single-frequency design: the
multifrequency conditioning used by Bustin et al.\ and Heras Rivera et al.\
would let the same network handle acquisitions at 30--120~Hz, broadening
clinical applicability. A third is that the present work has not produced
a published comparison wall-clock time against NLI, the headline number
in oNLI; that benchmark would be cheap to generate and should accompany
any clinical paper.

If validated in vivo, TSM-FNO would provide an MRE-derived activity index
for diseases where active lesion dynamics matter and where conventional
stiffness mapping is insensitive. The framework is general: substituting
the phantom distribution and the acoustoelastic prior would let the same
architecture be retargeted at glioma growth-direction
estimation~\cite{he2025glioma}, post-treatment monitoring of NAC response
in breast cancer~\cite{sinha2025breast}, or surgical-planning stiffness
heterogeneity in meningiomas.

% ─── Methods ──────────────────────────────────────────────────────────────────
\section*{Methods}
\label{sec:methods}

\subsection*{Forward problem and 1D analytical solution}
\label{sec:methods-1d}

For a time-harmonic shear wave at frequency $f$, the displacement
$u(x,t)=\Re\{\hat u(x)\,e^{-i\omega t}\}$ in a heterogeneous viscoelastic
1D medium of density $\rho$ and complex shear modulus $G(x)$ satisfies
equation~\eqref{eq:1d-helmholtz}. We choose the power-law profile
$G(x) = G_{0}(1+\alpha x)^{2}(1+i\xi)$ of
equation~\eqref{eq:G-profile} with values matched to in vivo brain MRE
(Methods,~\nameref{sec:methods-1d-params}).

\paragraph{Closed-form solution via Euler reduction.}
The substitution $s = 1+\alpha x$ maps the variable-coefficient operator
to constant-coefficient form. Computing
$\mathrm{d}u/\mathrm{d}x = \alpha\,\mathrm{d}u/\mathrm{d}s$,
$G(x)\,\mathrm{d}u/\mathrm{d}x = \alpha G_{0}(1+i\xi)\,s^{2}\,u_{s}$, and
$\mathrm{d}/\mathrm{d}x[G u_{x}] =
\alpha^{2}G_{0}(1+i\xi)\,(2s u_{s}+s^{2}u_{ss})$, the wave equation
becomes
\begin{equation}
s^{2}\,u_{ss}(s) + 2 s\,u_{s}(s) + \nu^{2}\,u(s) = 0,
\qquad
\nu^{2} = \frac{\rho\omega^{2}}{\alpha^{2}G_{0}(1+i\xi)},
\label{eq:euler}
\end{equation}
the Euler equation. The ansatz $u\propto s^{p}$ gives the indicial
equation $p^{2}+p+\nu^{2}=0$, whose roots
$p_{1,2}=-\tfrac{1}{2}\pm i\mu$ with $\mu=\sqrt{\nu^{2}-\tfrac{1}{4}}$ are
complex even when $\xi=0$ for the wave-regime $\nu^{2}>1/4$. The general
solution is therefore equation~\eqref{eq:1d-general}, with $A,B$ fixed by
the boundary conditions
\begin{equation}
A + B = u_{0},\qquad
A\,s_{L}^{p_{1}} + B\,s_{L}^{p_{2}} = 0,\quad s_{L}=1+\alpha L,
\label{eq:bcs}
\end{equation}
which solves to
\begin{equation}
A = \frac{u_{0}}{1 - s_{L}^{p_{1}-p_{2}}},\qquad
B = u_{0} - A.
\label{eq:AB}
\end{equation}
Equation~\eqref{eq:1d-general} with~\eqref{eq:AB} is the exact analytical
forward solution we use to validate the FD solver
(Fig.~\ref{fig:brain-forward}). The substitution and the indicial equation
together constitute the classical Liouville--Green reduction of
variable-coefficient Helmholtz operators to constant coefficients; the
power-law $(1+\alpha x)^{2}$ profile is precisely the class for which the
reduction terminates exactly without a WKB expansion.

\paragraph{Finite-difference numerical solver.}
On the uniform grid $x_{j}=j\Delta x$, $\Delta x=L/(N\!-\!1)$,
$j=0,\dots,N\!-\!1$, the standard second-order conservative
discretisation of equation~\eqref{eq:1d-helmholtz} is
\begin{equation}
-\frac{G_{j+1/2}(u_{j+1}-u_{j}) - G_{j-1/2}(u_{j}-u_{j-1})}{\Delta x^{2}}
- \rho\omega^{2}u_{j} = 0,
\label{eq:fd-1d}
\end{equation}
with $G_{j\pm 1/2}$ the arithmetic mean of the cell-centre values; the
first and last rows of the linear system are overwritten to enforce
$u_{0}=u_{0,\mathrm{BC}}$ and $u_{N-1}=0$. The resulting complex tridiagonal
system is solved by direct sparse factorisation. Convergence to
equation~\eqref{eq:1d-general} is second-order in $N$
(Fig.~\ref{fig:brain-forward}d), reaching relative $L^{2}$ error
$6\!\times\!10^{-4}$ at $N=1024$.

\paragraph{In-vivo-matched parameter ranges.}
\label{sec:methods-1d-params}
Defaults: $L=0.16$~m, $G_{0}=1.5$~kPa, $G(L)=3.5$~kPa giving
$\alpha = (\sqrt{G(L)/G_{0}}-1)/L \approx 3.30~\mathrm{m^{-1}}$,
$\xi=0.10$, $\rho=10^{3}$~kg/m$^{3}$, $f=50$~Hz, $u_{0}=1$ (unit driver).
These match the white-matter / deep-grey transition and pneumatic-driver
frequencies used in the brain MRE
literature~\cite{murphy2024lowfreq,herasrivera2025onli}.

\paragraph{1D ablation: architectures and loss functions.}
\label{sec:methods-1d-ablation}
The ablation in Fig.~\ref{fig:arch-loss} crosses two architectures and
four loss functions. \emph{Architectures.} The FNO is identical to the
one trained for Fig.~\ref{fig:brain-inverse} (3 spectral-conv blocks,
width 24, 12 Fourier modes, 23{,}282 parameters). The SIREN is a 1D
convolutional sinusoidal representation network: a $1\!\times\!1$ lift
to width 24, then three layers of \texttt{Conv1d(kernel\,=\,11)} each
followed by $\sin(\omega_{0}\cdot)$ activation with $\omega_{0}=30$ and
SIREN initialisation (first layer uniform in
$[-1/n_{\text{in}},1/n_{\text{in}}]$, subsequent layers uniform in
$[-\sqrt{6/n_{\text{in}}}/\omega_{0},+\sqrt{6/n_{\text{in}}}/\omega_{0}]$),
ending in a linear $1\!\times\!1$ projection to the two-channel output.
This gives 19{,}226 parameters, $\approx 0.83\times$ the FNO budget.
\emph{Losses.} (i) Per-sample relative $L^{2}$ on the normalised $G$
(the default in Figs.~\ref{fig:brain-inverse}--\ref{fig:mixed-training}).
(ii) Mean-squared error on the normalised $G$. (iii) The 1D Helmholtz
residual on the predicted $G$,
\[
\mathcal{L}_{\mathrm{pde}}=
\Bigl\langle\bigl|\,G_{\mathrm{pred}}\cdot\Delta_{x}u
       + \rho\omega^{2}u\,\bigr|\,\big/\,
\bigl(\rho\omega^{2}|u|+\epsilon_{0}\bigr)\Bigr\rangle_{\text{interior}},
\]
with $\Delta_{x}$ the three-point FD Laplacian on the same grid as the
forward solver; this loss requires no ground-truth $G$ and rescales the
prediction by $G_{\text{scale}}$ before evaluation. (iv) The composite
$\mathcal{L}_{\mathrm{rl2}}+\lambda_{\mathrm{pde}}\mathcal{L}_{\mathrm{pde}}$
with $\lambda_{\mathrm{pde}}=0.05$, matching the weight used in the 2D
pipeline. All eight configurations share the data, batch size, optimiser
(AdamW, $\mathrm{lr}=10^{-3}$, cosine schedule to $10^{-5}$), and
training-set size; only the architecture and loss differ.

\paragraph{1D FNO inversion.}
\label{sec:methods-1d-fno}
A 1D Fourier neural operator (3 spectral-conv blocks, width 24, 12
Fourier modes, $23{,}282$ parameters) maps two real input channels
$(\Re\,\hat u, \Im\,\hat u)$ to two real output channels
$(\Re\,G, \Im\,G)/G_{\mathrm{scale}}$ on a $N=96$ grid. Training samples
draw $G_{0}\!\sim\!U(0.8,2.5)$~kPa, $G(L)\!\sim\!U(1.5,4.5)$~kPa,
$\xi\!\sim\!U(0.05,0.20)$, with complex Gaussian noise added to $\hat u$ at
SNR$\,=\,25$~dB and per-sample normalisation $\|\hat u\|_{\infty}=1$. The
loss is per-sample relative $L^{2}$ on $G$; optimisation is AdamW at
$\mathrm{lr}=10^{-3}$ with cosine annealing for 25 epochs over
$1{,}500$ training samples and $300$ held-out validation samples. Final
validation RL$^{2}$ is $0.043$. The trained operator generalises across
the full prior with the loss curves shown in
Fig.~\ref{fig:brain-inverse}a and per-sample predictions in
Fig.~\ref{fig:brain-inverse}b. All training scripts and the resulting
checkpoint are in \texttt{paper/figures/}.

\paragraph{Symmetric mirrored-Euler analytical solution.}
\label{sec:methods-1d-sym}
For the symmetric profile of equation~\eqref{eq:G-sym} the same Euler
reduction applies on each half. With $\zeta = x - L/2$ and $|\zeta| \ge 0$,
the substitution $s = 1+\alpha|\zeta|$ yields the Euler equation in both
halves with the same indicial roots $p_{1,2}=-\tfrac{1}{2}\pm i\mu$,
$\mu=\sqrt{\nu_{c}^{2}-\tfrac{1}{4}}$, $\nu_{c}^{2}=\rho\omega^{2}/[\alpha^{2}G_{c}(1+i\xi)]$.
Writing
$u_{L}(s)=A_{L}s^{p_{1}}+B_{L}s^{p_{2}}$ on $x\in[0,L/2]$ and
$u_{R}(s)=A_{R}s^{p_{1}}+B_{R}s^{p_{2}}$ on $x\in[L/2,L]$ (each with
$s=1+\alpha|\zeta|$), the four coefficients are fixed by
\begin{align}
A_{L}\,s_{b}^{p_{1}} + B_{L}\,s_{b}^{p_{2}} &= u_{0}, \quad s_{b}=1+\alpha L/2,\nonumber\\
A_{R}\,s_{b}^{p_{1}} + B_{R}\,s_{b}^{p_{2}} &= 0,\nonumber\\
A_{L} + B_{L} - A_{R} - B_{R} &= 0,\quad\text{(continuity of }u\text{ at }L/2)\nonumber\\
p_{1}(A_{L}+A_{R}) + p_{2}(B_{L}+B_{R}) &= 0,\quad\text{(continuity of traction)}
\label{eq:sym-bc}
\end{align}
where the last equation absorbs the sign flip
$\mathrm{d}s/\mathrm{d}x = -\alpha$ on $x<L/2$. The $4\!\times\!4$ system
\eqref{eq:sym-bc} has a unique solution for any $\nu_{c}^{2}\neq 0$ and is
used as the closed-form reference against which the FD solver and the
trained FNO are tested (Fig.~\ref{fig:symmetric-stress}). Spatial
convergence is again $\mathcal{O}(N^{-2})$, reaching relative $L^{2}$
error $5\!\times\!10^{-4}$ at $N=1024$. For the FNO stress test we
sample $G_{c}\!\sim\!U(0.8,2.5)$~kPa and $G_{b}\!\sim\!U(1.5,4.5)$~kPa
matching the per-endpoint ranges used in training; the operator's
trained weights are evaluated zero-shot without retraining or
fine-tuning.

\paragraph{Quasi-static limit and dimensional argument for TSM.}
In the static limit $\omega\to 0$, equation~\eqref{eq:1d-helmholtz} reduces
to $\mathrm{d}\sigma/\mathrm{d}x=0$ where
$\sigma(x)=G(x)[\mathrm{d}u/\mathrm{d}x - \eps^{*}(x)]$ is the stress and
$\eps^{*}$ denotes a localised eigenstrain. The stress is therefore
spatially constant, $\sigma\equiv\sigma_{\mathrm{bar}}$. Integrating with
the boundary condition $u(0)=u(L)$ (periodic / clamped equivalent) and
$\int_{0}^{L}\eps^{*}\,\mathrm{d}x=\eps_{0}\,\ell$ for a perturbation of
width $\ell$ inside a domain of length $L$ gives
\begin{equation}
\sigma_{\mathrm{bar}} = -\eps_{0}\,\ell\, \Bigg/
\int_{0}^{L}\!\frac{\mathrm{d}x}{G(x)}.
\label{eq:static}
\end{equation}
For any bounded $G(x)$ the integral on the right grows linearly with $L$,
so $\sigma_{\mathrm{bar}}\sim O(\ell/L)\to 0$ as $L/\ell\to\infty$. The
1D static stress concentration around a localised eigenstrain therefore
vanishes in the infinite-domain limit; the perilesional ring observed in
TSM is a strictly two- or three-dimensional phenomenon, driven by the
divergence of $G\nabla u$ being a vector rather than a scalar
constraint. The 1D problem cannot reproduce it---a structural reason to
move the operator to 2D.

\subsection*{Forward problem in 2D and the Eshelby form}
\label{sec:methods-2d}

The 2D Helmholtz problem~\eqref{eq:2d-helmholtz} is discretised on a
uniform $N\!\times\!N$ grid by a 5-point finite-difference Laplacian and
solved via a sparse direct factorisation. For phantoms with an
acoustoelastic inclusion the operator picks up an Eshelby eigenstrain
term: writing the strain decomposition
$\eps_{\mathrm{tot}}=\eps_{\mathrm{el}}+\bar\eps^{*}\mathbb{1}_{\mathrm{lesion}}$
with $\bar\eps^{*}=A\,(\Dsig/\Gbg)$, the equilibrium equation becomes
\begin{equation}
\nabla\!\cdot\!\bigl[G^{*}(\xvec)\,(\nabla u - \bar\eps^{*})\bigr]
+ \rho\omega^{2}\,u = 0,
\label{eq:eshelby}
\end{equation}
where $G^{*}(\xvec)=G(\xvec)(1+i\xi)$ is the complex shear modulus. The
analytical Lam\'e pre-stress around a circular pressurised inclusion of
equivalent radius $\aeq$,
\begin{equation}
\sigma_{rr}(r) = -p\,(\aeq/r)^{2},\quad
\sigma_{\theta\theta}(r) = +p\,(\aeq/r)^{2},\quad
\Dsig(r) = 2p\,(\aeq/r)^{2},
\label{eq:lame}
\end{equation}
is the source of the perilesional strain signal. We use
$\Dsig(\xvec)/\Gbg$ directly as one of the network's input channels, so
the operator does not have to learn the analytical decay.

\subsection*{Spectral convolution and FNO architecture}
\label{sec:methods-fno}

A spectral convolution layer maps a hidden field
$v\in\C^{w\times N\times N}$ to
\begin{equation}
(K v)(\xvec) =
\IFT\!\Bigl[\,R\cdot\FT[v]\,\Bigr](\xvec),
\label{eq:spectral}
\end{equation}
where $\FT$ is the discrete Fourier transform along the spatial
dimensions, $R\in\C^{w\times w\times m_{1}\times m_{2}}$ is a learned
complex weight tensor restricted to the lowest $m_{1}\times m_{2}$ Fourier
modes, and the unaffected high-frequency modes are set to zero before the
inverse transform. A Fourier block composes the spectral convolution with
a $1\!\times\!1$ pointwise channel mixer $W$ and a GELU
nonlinearity, giving the residual update
$v \mapsto \mathrm{GELU}(Kv+Wv)$. TSM-FNO stacks four such blocks of
width $w=48$ and truncation $m_{1}=m_{2}=16$, preceded by a $1\!\times\!1$
lift from $4{+}2$ input channels (the four physical channels plus two
coordinate channels appended internally) to width 48, and followed by
three task heads as described in the main text. The full network has
4{,}738{,}371 parameters.

\subsection*{Loss function}
\label{sec:methods-loss}

Define $\|\cdot\|_{2}$ as the spatial $L^{2}$ norm and let
$M_{\mathrm{ring}}$ denote the boolean perilesional-shell mask
(thickness 5~mm, computed from the lesion mask by an Euclidean distance
transform). The six terms of equation~\eqref{eq:loss} are
\begin{align*}
\mathcal{L}_{G} &= \tfrac{1}{B}\sum_{b}
    \|G^{(b)}_{\mathrm{pred}}-G^{(b)}\|_{2} /
    (\|G^{(b)}\|_{2}+\eps_{0}),\\
\mathcal{L}_{\eps} &= \mathrm{MSE}(\eps_{\mathrm{pred}},\eps),\\
\mathcal{L}^{\mathrm{ring}}_{\eps} &=
    \tfrac{1}{B}\sum_{b}
    \|(\eps^{(b)}_{\mathrm{pred}}-\eps^{(b)})\odot M_{\mathrm{ring}}\|_{2}/
    (\|\eps^{(b)}\odot M_{\mathrm{ring}}\|_{2}+\eps_{\mathrm{floor}}),\\
\mathcal{L}_{\mathrm{ac}} &=
    \mathrm{MSE}\bigl(\,(G_{\mathrm{pred}}-\Gbg)\odot M_{\mathrm{ring}},\;
    |A_{\mathrm{pred}}|\,\eps_{\mathrm{pred}}\,\Gbg\odot M_{\mathrm{ring}}\bigr),\\
\mathcal{L}_{\mathrm{pde}} &=
    \mathrm{mean}\!\left(
    \frac{|G_{\mathrm{pred}}\Delta u + \rho\omega^{2}u|}
         {\rho\omega^{2}|u|+\eps_{0}}
    \right)\Big|_{\mathrm{interior}},\\
\mathcal{L}_{\mathrm{exp}} &=
    \mathrm{BCE}\!\bigl(\sigma\!\bigl[(s-\tau)/\tau\bigr],
    \mathbb{1}_{\mathrm{exp}}\bigr),\quad
    s=\max(\eps_{\mathrm{pred}}\odot M_{\mathrm{ring}}),
\end{align*}
with weights
$(\lambda_{\eps},\lambda_{\mathrm{ring}},\lambda_{\mathrm{ac}},
\lambda_{\mathrm{pde}},\lambda_{\mathrm{exp}})=(1.0,0.5,0.1,0.05,0.2)$.
The acoustoelastic term enforces the in-shell consistency
$\Geff-\Gbg=|A|\eps\Gbg$ that follows from equation~\eqref{eq:eshelby}.
The PDE residual uses the same 5-point Laplacian as the forward solver
and provides an unsupervised gradient on $G_{\mathrm{pred}}$ even where
ground-truth $G$ is absent.

\subsection*{Training and evaluation}
\label{sec:methods-train}

The training set is 50{,}000 synthetic samples generated by 100-task SLURM
arrays on the NCSA Delta system. Training runs for 100 epochs with the
AdamW optimiser, learning rate $10^{-3}$, batch size 24, cosine learning-rate
schedule to $10^{-5}$, gradient clipping at norm 1.0, on a single
NVIDIA A100 GPU; wall clock is approximately 40 minutes. The held-out
validation split is the last 10\% of samples. Evaluation metrics are
relative $L^{2}$ on whole field and ring; SSIM on $G$; expansion classifier
AUC; and recovered $A$ error.

\subsection*{Phase-A phantom validation protocol}
\label{sec:methods-phaseA}

The Phase-A protocol uses a deterministic single circular inclusion of
half-width $a=12$~voxels (36~mm at 3~mm voxel pitch), centred in an
$80\!\times\!80$ domain, with $\Gbg=1500$~Pa, $G_{\mathrm{lesion}}=5000$~Pa,
zero pressure, and ten random boundary sources at 80~Hz. The trained
operator is applied to the resulting wave field along with its
analytically-computed physics-informed channels, and metrics are computed
relative to the analytical ground truth. Pass criteria are Pearson
$r\ge 0.85$ for $G_{\mathrm{pred}}$ vs.\ ground truth and
$|\eps_{\mathrm{pred}}|_{\mathrm{mean,outside}}\le 0.05$ for the strain head.
The validation script is at
\texttt{tsm\_fno/scripts/validate\_phantom.py} in the project repository.

\subsection*{Code and data availability}
\label{sec:methods-availability}

All source code for the forward solver, phantom generator, FNO architecture,
training, and validation is available at
\url{https://github.com/nahilsobh/Eman_Richard} under an open-source licence.
The synthetic training datasets ($\sim 14$~GB) are not redistributed but are
regenerated bit-for-bit by the included SLURM submission scripts with a fixed
random seed.

% ─── Figures ──────────────────────────────────────────────────────────────────
\clearpage
\section*{Figures}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig1_brain_forward.png}
\caption{Brain-scale 1D forward problem: analytical anchor and numerical
convergence. See legend below.}
\label{fig:brain-forward}
\end{figure}

\IfFileExists{figures/fig2a_loss.png}{%
\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig2a_loss.png}\\[6pt]
\includegraphics[width=\linewidth]{figures/fig2b_predict.png}
\caption{1D FNO inversion: training curves and per-sample predictions.}
\label{fig:brain-inverse}
\end{figure}
}{}

\IfFileExists{figures/fig7_smooth_symmetric.png}{%
\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig7_smooth_symmetric.png}
\caption{Smooth symmetric brain-scale profile $G(x)=G_c+\beta(x-L/2)^2$:
$C^\infty$, no absolute-value kink.}
\label{fig:smooth-symmetric}
\end{figure}
}{}

\IfFileExists{figures/fig3_symmetric.png}{%
\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig3_symmetric.png}
\caption{Symmetric mirrored-Euler stress test: forward analytical/numerical
agreement (a--c) and zero-shot FNO inverse evaluation (d, e--h).}
\label{fig:symmetric-stress}
\end{figure}
}{}

\IfFileExists{figures/fig4b_mixed_bars.png}{%
\begin{figure}[!ht]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig4a_mixed_loss.png}\\[6pt]
\includegraphics[width=0.85\linewidth]{figures/fig4b_mixed_bars.png}\\[6pt]
\includegraphics[width=\linewidth]{figures/fig4c_mixed_predict.png}
\caption{Mixed-distribution training closes the symmetric gap.}
\label{fig:mixed-training}
\end{figure}
}{}

\IfFileExists{figures/fig5b_arch_loss_heatmap.png}{%
\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig5a_arch_loss_curves.png}\\[6pt]
\includegraphics[width=\linewidth]{figures/fig5b_arch_loss_heatmap.png}\\[6pt]
\includegraphics[width=\linewidth]{figures/fig5c_arch_loss_predict.png}
\caption{Architecture and loss-function ablation: FNO vs SIREN with four
loss functions.}
\label{fig:arch-loss}
\end{figure}
}{}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig4_phaseA.png}
\caption{Phase-A phantom validation panel.}
\label{fig:phaseA}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{figures/fig5_cohort.png}
\caption{Synthetic cohort performance.}
\label{fig:cohort}
\end{figure}

\clearpage
\section*{Figure legends}

\textbf{Figure 1. Brain-scale 1D forward problem: analytical anchor and
numerical convergence.}
(a) Continuous complex shear modulus
$G(x)=G_{0}(1+\alpha x)^{2}(1+i\xi)$ at brain scale ($L=16$~cm,
$G$ ramping 1.5~$\to$~3.5~kPa, $\xi=0.10$); real and imaginary parts
shown separately. (b) Closed-form analytical solution
$u(s)=A s^{p_{1}}+B s^{p_{2}}$ of the Euler-reduced
Helmholtz equation~\eqref{eq:1d-helmholtz} at $f=50$~Hz with Dirichlet
boundary conditions; real and imaginary parts. (c) Second-order
finite-difference solver (markers, $N=512$) overlaid on the analytical
solution (line). (d) Spatial convergence of the FD solver to the
analytical reference: relative $L^{2}$ error decays at the expected
$\mathcal{O}(N^{-2})$ rate, reaching $6\!\times\!10^{-4}$ at $N=1024$.

\textbf{Figure 2. 1D FNO inversion: training and prediction.}
(a) Training and held-out validation relative-$L^{2}$ loss versus epoch
for a 23k-parameter 1D Fourier neural operator (3 spectral blocks,
width 24, 12 modes) trained to map noisy $\hat u(x)$ at SNR\,=\,25\,dB
to the complex shear modulus $G(x)$ on samples drawn from the
brain-scale parameter prior. (b) Four held-out samples: ground-truth
and predicted real (top row) and imaginary (bottom row) parts of
$G(x)$; the FNO recovers both the smooth ramp and the loss component.

\textbf{Figure 3. Smooth symmetric profile
$G(x)=G_c+\beta(x-L/2)^2$.}
(a) Complex shear modulus across the head (real and imaginary parts).
(b) First derivative $\mathrm{d}G/\mathrm{d}x$ continuous and zero at
$x=L/2$; second derivative $G''=2\beta$ constant. No
absolute-value-induced discontinuities. (c) Forward solution at
$f=50$~Hz: analytical power series (line) and FD solver (markers,
$N=512$) agree pointwise. (d) Spatial convergence of the FD solver:
$\mathcal{O}(N^{-2})$ to relative $L^{2}$ error
$1.8\!\times\!10^{-4}$ at $N=2048$. (e) Imaginary part of the analytical
and FD solutions. (f) Pointwise residual at $N=512$.

\textbf{Figure 4. Symmetric mirrored-Euler stress test.}
The 1D FNO is evaluated zero-shot on the symmetric profile
$G_{\mathrm{sym}}(x)=G_{c}(1+\alpha|x-L/2|)^{2}(1+i\xi)$. (a) Two
representative symmetric profiles, real parts. (b) Analytical solution
(line) and FD solver (markers, $N=512$) on one such sample. (c) Spatial
convergence: relative $L^{2}$ error of the FD solver decays at
$\mathcal{O}(N^{-2})$ to $5\!\times\!10^{-4}$ at $N=1024$. (d) Per-sample
RL$^{2}$ histogram over 300 samples: in-distribution (asymmetric
training family, mean $0.05$) versus the symmetric zero-shot evaluation
(mean $0.21$). The FNO degrades gracefully but cannot fully resolve the
central kink. (e--h) Four sample predictions: black is ground truth, blue
is FNO real-part prediction, red is FNO imaginary-part prediction; the
broad shape and the imaginary component are recovered while the kink at
$x=L/2$ is smoothed.

\textbf{Figure 5. Mixed-distribution training closes the symmetric gap.}
Retraining the same 23k-parameter 1D FNO on a 50/50 mixture of
asymmetric and symmetric samples. (a) Training loss and held-out
validation loss tracked separately on each family. (b) Mean per-sample
RL$^{2}$ on each test family for the two models: the asymmetric-only
specialist achieves $0.043$ on asym and $0.205$ on sym, whereas the
mixed generalist achieves $0.088$ on asym and $0.046$ on sym -- the
symmetric gap is fully closed at the cost of a $2\!\times$ degradation
on the asymmetric family. (c) Four mixed-trained predictions on
symmetric samples; the kink at $x=L/2$ is now resolved.

\textbf{Figure 6. Architecture and loss-function ablation: FNO vs SIREN
with four loss functions.}
Both architectures (FNO with 23k params, SIREN with 19k params) trained
on identical 1{,}500 asymmetric brain-scale samples for 15 epochs.
(a) Validation RL$^2$ vs epoch on the asymmetric (left) and symmetric
(right) test sets, eight curves coloured by architecture and
line-styled by loss. (b) Final mean RL$^2$ heatmaps for the two test
sets across the 2$\times$4 grid. (c) Per-sample predictions for the
four most informative configurations on one asymmetric (top) and one
symmetric (bottom) held-out sample. SIREN+RL$^2$ achieves the lowest
symmetric-OOD error (0.139), while FNO+RL$^2$+$\lambda$\,PDE is the best
FNO configuration. Pure PDE residual is under-constrained as a sole
loss and plateaus at RL$^2\!\approx\!0.7$ for both architectures.

\textbf{Figure 7. TSM-FNO architecture.}
Schematic of the four-block 2D Fourier neural operator with the
$4+2$-channel input lift, spectral convolutions truncated to 16 Fourier
modes per dimension, and the three task heads ($G$, $\eps$, $A$).

\textbf{Figure 8. Acoustoelastic phantom physics.}
(a) Lam\'e analytical pre-stress field $\Dsig(\xvec)$ around a pressurised
elliptical inclusion. (b) Perilesional shell mask used in the ring loss.
(c) Resulting wave field $\mathrm{Re}\,u$ at 80~Hz with directional
filtering. (d) Ground-truth strain field $\eps_{\mathrm{latent}}$.

\textbf{Figure 9. Phase-A phantom validation.}
Four-panel comparison on the deterministic static 2D phantom:
ground-truth $G$, TSM-FNO $G_{\mathrm{pred}}$, direct inversion $G_{\mathrm{DI}}$,
and TSM-FNO $\eps_{\mathrm{pred}}$. White contour marks the lesion boundary.

\textbf{Figure 10. Synthetic cohort performance.}
(a) Stiffness recovery RL$^{2}$ stratified by lesion pressure, contrast
$G_{\mathrm{lesion}}/\Gbg$, and equivalent radius across 5{,}000 held-out
samples. (b) Perilesional-shell RL$^{2}$ on $\eps$ versus pressure bin --
the strain-head metric. (c) Headline cohort metrics
(RL$^{2}$ on $G$, ring RL$^{2}$, SSIM, AUC).

\bibliography{refs}

\end{document}
